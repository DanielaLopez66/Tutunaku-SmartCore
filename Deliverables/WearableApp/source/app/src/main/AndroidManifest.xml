<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".TutunakuWearApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.DeviceDefault"
        tools:targetApi="31">

        <!--
          Traffic en texto plano (HTTP) SOLO para apuntar al backend de
          desarrollo (ver network_security_config.xml). Para producción,
          servir el backend con HTTPS y quitar usesCleartextTraffic.
        -->
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Recibe el traspaso de sesión desde el teléfono (path "/tutunaku/auth")
          vía la Wearable Data Layer API — ver data/wear/PhoneAuthListenerService.kt
          y data/wear/WatchSyncManager.kt (declara la capability compartida con
          el teléfono de forma programática, no aquí).
        -->
        <service
            android:name=".data.wear.PhoneAuthListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/tutunaku/auth" />
            </intent-filter>
        </service>
    </application>
</manifest>

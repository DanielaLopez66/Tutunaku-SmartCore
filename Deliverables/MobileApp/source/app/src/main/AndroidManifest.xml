<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Notificación local cuando el reloj manda un mensaje de XP ganado
         (ver data/wear/WatchMessageListenerService.kt). Se pide en tiempo
         de ejecución tras el login, no aquí. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TutunakuMobileApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        tools:targetApi="31">

        <!--
          Traffic en texto plano (HTTP) SOLO para apuntar al backend de
          desarrollo (ver network_security_config.xml). Para producción,
          servir el backend con HTTPS y quitar usesCleartextTraffic.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Recibe mensajes del reloj (ej. "/tutunaku/xp_gained") vía la
          Wearable Data Layer API — ver data/wear/WatchMessageListenerService.kt
          y data/wear/WatchSyncManager.kt (declara la capability compartida
          con el reloj de forma programática, no aquí).
        -->
        <service
            android:name=".data.wear.WatchMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
            </intent-filter>
        </service>
    </application>
</manifest>
